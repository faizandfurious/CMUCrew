<%= simple_form_for(@lineup) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :boat_id %>
    <%= f.input :name %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
