<!-- <h3>Sign in</h3>

<p>
<%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
  <div><%= f.email_field :email, :placeholder => 'tartan@crew.com' %></div>

  <div><%= f.password_field :password, :placeholder => 'password' %></div>


  <% if devise_mapping.rememberable? -%>
    <div><%= f.check_box :remember_me %> <%= f.label :remember_me %></div>
  <% end -%>

  <div><%= f.submit "Sign in" %></div>
<% end %>
</p>

<%= render "links" %> -->

<div class="alert alert-error">
  <strong>Notice:</strong> This functionality is not yet available! Check back soon.
</div>

<h3>Sign in</h3>

<p>
<%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
  <div><%= f.email_field :email, :placeholder => 'tartan@crew.com', :disabled => true %></div>

  <div><%= f.password_field :password, :placeholder => 'password', :disabled => true %></div>


  <% if devise_mapping.rememberable? -%>
    <div><%= f.check_box :remember_me, :disabled => true %> <%= f.label :remember_me %></div>
  <% end -%>

  <div><%= f.submit "Sign in", :disabled => true %></div>
<% end %>
</p>
<!-- 
<%= render "links" %> -->