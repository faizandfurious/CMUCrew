<% if user_signed_in? %>
  <li>
  	<%= link_to('Logout', destroy_user_session_path, :method => :delete) %>        
  </li>
<% else %>
  <li>
  	<%= link_to('Member\'s Area', new_user_session_path)  %>  
  </li>
<% end %>