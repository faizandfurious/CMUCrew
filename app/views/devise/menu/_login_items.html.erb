<% if user_signed_in? %>
  <li>
  	<%= link_to('Logout', destroy_user_session_path, :method => :delete, :class => "account") %>        
  </li>
<% else %>
  <li class="account">
  	<%= link_to('Member\'s Area', root_path, :class => "account")  %>  
  </li>
<% end %>